apiVersion: apps/v1
kind: Deployment
metadata:
  name: whitelabelstorefront-backend
  labels:
    app: whitelabelstorefront-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: whitelabelstorefront-backend
  template:
    metadata:
      labels:
        app: whitelabelstorefront-backend
    spec:
      containers:
        - name: whitelabelstorefront-backend
          image: whitelabelstorefront-backend:node18
          ports:
            - containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: whitelabelstorefront-backend
spec:
  selector:
    app: whitelabelstorefront-backend
  ports:
    - port: 80
      targetPort: 3000
