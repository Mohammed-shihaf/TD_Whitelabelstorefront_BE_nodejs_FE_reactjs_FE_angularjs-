tool: nyc
language: javascript
language_version: "22"
technique: "Statement / Branch Coverage"
target_files: [../../src/]
run:
  command: "npx nyc --reporter=text mocha 'test/**/*.test.js'"
expects:
  coverage_report_generated: true
verified_in_session: true
verified_result: "81.25% stmts / 50% branch / 66.66% funcs / 84.61% lines against the real app.js; lines 24-25 (the require.main listen() guard, not exercised by the request tests) uncovered"
